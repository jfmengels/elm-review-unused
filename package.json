{
  "name": "",
  "version": "1.0.0",
  "scripts": {
    "test": "elm make --docs=/tmp/docs.json && elm-format src/ --validate && elm-test && elm-review"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-review": "^1.0.2",
    "elm-test": "^0.19.1-revision2"
  }
}
